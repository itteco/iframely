- copy static from /static
- render script with injector wrapper and dependencies
- validate urlMatch if no re in plugin
- plugin handler timeout
- tie rel from data to nested link.
- support empty test plugin (only test for domain)
- $oembed_html - requirement (frequently used)
- invalid content type provider
- plugin errors - show as list in debug tool
- test framework
- canonicals workaround
- domain (no re) plugin: if mixins not worked - use all generic plugins?
- validate data to be a string
- unify meta.date field (parse date?)
- create "og" an "twitter" plugins to provide this requirements, to skip if (!meta.og) checks. Solve problem with domain mixins.
- fallback if domain plugin not work on 100%
- debug: option for load reader
- debug: show overriden meta and sources
- customize mixins (add specific rel, widht, height)
    - in collegehumor
    - in video.yandex (https for oembed src)
- replace rel=thumbnail with rel=image if image size more then 500px

Images:
- downgrade image header size?
- do not load favicon size
- check if favicon exists

Not implemented providers:

- dropbox (render)
- flickr.set flickr.gallery (render)
- github .repo .author (script)
- justin.tv
    - channel not allowed in iframe.
    - og video in archive page - always autoplay
- listly (embeded script - not tested yet)
- mixcloud (oembed removed from site)
- video.nymag.com - bug with iframe size (inside video too large)
- pinterest board - need render gallery


